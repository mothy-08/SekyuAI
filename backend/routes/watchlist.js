import { Router } from 'express';

const router = Router();

// GET /api/watchlist
router.get('/', (req, res) => {});

// POST /api/watchlist
router.post('/', (req, res) => {});

// DELETE /api/watchlist/:repo
router.delete('/:repo', (req, res) => {});

export default router;
